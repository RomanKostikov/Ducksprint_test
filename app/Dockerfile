# Используем официальный образ Python в качестве базового
FROM python:3.10-slim

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файл зависимостей в контейнер
COPY requirements.txt .

# Устанавливаем зависимости
RUN pip install --no-cache-dir -r requirements.txt

# Копируем остальной код приложения в контейнер
COPY . .

# Если ваше приложение использует порты (например, Flask), откройте их
EXPOSE 8000

# Устанавливаем переменные окружения (при необходимости)
# Лучше передавать их через docker-compose или файл .env

# Команда для запуска приложения
CMD ["python", "main.py"]
